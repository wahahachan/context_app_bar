name: example
description: A new Flutter project.
version: 1.0.0
publish_to: none

environment:
  sdk: '^3.6.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  context_app_bar: ">=1.0.0"

flutter:
  uses-material-design: true